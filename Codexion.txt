do struct that had has every substruct for every context, like Parse etc avoids singleton

parsing 

Reminder: All arguments are mandatory. Reject invalid inputs such
as negative numbers, non-integers, or a scheduler other than fifo or
edf.

check valgrind on overflow

still need to check who allows a 0 or not between these 

long           time_to_compile;
long           time_to_debug;
long           time_to_refactor;
long           number_of_compiles_required;

fix main, do a thread loop, sketch dongles around coders (come up with wraparound formula)
no need for available and taken, can use sleep and mutexes

1. Try to get both dongles (left and right).
2. Once it has both — compile, for time_to_compile ms.
3. Put both dongles back.
4. Debug, for time_to_debug ms.
5. Refactor, for time_to_refactor ms.
6. Loop.
